server {
	
	listen 8080;
	server_name _;
	
	root /usr/share/nginx/html;
	index index.html;


	# Proper redirection
	port_in_redirect off;
	absolute_redirect off;

	location / {

		try_files $uri $uri/ =404;
	}
}

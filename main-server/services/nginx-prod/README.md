# 🌍 Production Nginx (Hugo Site)


## Overview

This service hosts the production version of a static Hugo website.

It is the public-facing endpoint of the CI/CD pipeline, where validated changes from the staging environment are deployed.

The site is publicly accessible over HTTPS through the reverse proxy.
